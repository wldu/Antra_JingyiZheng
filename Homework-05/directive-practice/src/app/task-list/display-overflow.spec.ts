import { DisplayOverflow } from './display-overflow';

describe('DisplayOverflow', () => {
  it('should create an instance', () => {
    const directive = new DisplayOverflow();
    expect(directive).toBeTruthy();
  });
});
